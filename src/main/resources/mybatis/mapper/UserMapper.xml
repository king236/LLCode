<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.learn.housePrice.mapper.UserMapper" >
    <resultMap id="BaseResultMap" type="com.learn.housePrice.entity.UserEntity" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="nickName" property="nickName" jdbcType="VARCHAR" />
        <result column="pswd" property="pswd" jdbcType="VARCHAR" />     
        <result column="email" property="email" jdbcType="VARCHAR" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, nickName, email, pswd
    </sql>

    <select id="getAll" resultMap="BaseResultMap"  >
       SELECT 
       <include refid="Base_Column_List" />
       FROM u_user
    </select>

    <select id="getOne" parameterType="java.lang.Long" resultMap="BaseResultMap" >
        SELECT 
       <include refid="Base_Column_List" />
       FROM u_user
       WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.learn.housePrice.entity.UserEntity" >
       INSERT INTO 
            u_user
            (nickName,pswd,email) 
        VALUES
            (#{nickName}, #{pswd}, #{email})
    </insert>

    <update id="update" parameterType="com.learn.housePrice.entity.UserEntity" >
       UPDATE 
            u_user
       SET 
        <if test="nickName != null">nickName = #{nickName},</if>
        <if test="pswd != null">pswd = #{pswd},</if>
        <if test="email != null">email = #{email},</if>
        nickName = #{nickName}
       WHERE 
            id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long" >
       DELETE FROM
             u_user 
       WHERE 
             id =#{id}
    </delete>
</mapper>